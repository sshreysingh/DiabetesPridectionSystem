package com.diabetes.prediction.ui.theme

import androidx.compose.ui.graphics.Color

// ── Medical Brand Colors ──────────────────────────────────────────────────────
val MedicalBluePrimary   = Color(0xFF1976D2) // Classic reassuring blue
val MedicalBlueDark      = Color(0xFF115293)
val MedicalBlueLight     = Color(0xFF4791DB)
val MedicalBlueBg        = Color(0xFFE3F2FD) // Very light blue for subtle backgrounds

// ── Secondary / Accent Colors ─────────────────────────────────────────────────
val AccentTeal           = Color(0xFF00897B) // Standard health teal

// ── Semantic Colors (Success / Warning) ───────────────────────────────────────
val SuccessGreen         = Color(0xFF2E7D32)
val SuccessGreenBg       = Color(0xFFE8F5E9)

val WarningRed           = Color(0xFFD32F2F)
val WarningRedBg         = Color(0xFFFFEBEE)

// ── Backgrounds & Surfaces ────────────────────────────────────────────────────
val BackgroundLight      = Color(0xFFF8F9FA) // Standard material light grey background
val SurfaceWhite         = Color(0xFFFFFFFF)

// ── Text Colors ───────────────────────────────────────────────────────────────
val TextPrimaryDark      = Color(0xFF202124) // Almost black for high readability
val TextSecondaryDark    = Color(0xFF5F6368) // Standard gray for secondary text
val TextHintDark         = Color(0xFF80868B)

// ── Dividers & Borders ────────────────────────────────────────────────────────
val BorderLight          = Color(0xFFE0E0E0)
